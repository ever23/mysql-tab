version: '3.8'

services:
  mariadb-test:
    image: mariadb:latest
    container_name: mariadb-test
    environment:
      - MARIADB_ROOT_PASSWORD=secret
      - MARIADB_DATABASE=test_db
    ports:
      - "3306:3306"
